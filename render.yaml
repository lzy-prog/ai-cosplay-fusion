services:
  - type: web
    name: ai-cosplay-fusion-backend
    env: python
    buildCommand: pip install -r backend/requirements_lite.txt
    startCommand: python backend/app_lite.py
    plan: free
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000
